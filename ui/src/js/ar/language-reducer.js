
const defaultState = require("../../languages/en-us.json");

export default function reducer(state=defaultState, action) {
  
  switch (action.type) {

    default: {
      return state;
    }

  }

}

